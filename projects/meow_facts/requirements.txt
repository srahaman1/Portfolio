# Meowfacts Fetcher — Dependencies
# Python 3.8+
#
# Install with:  pip install -r requirements.txt
#
# All other modules used (json, os, datetime) are part of the Python
# standard library and do not need to be installed separately.

requests>=2.28.0
